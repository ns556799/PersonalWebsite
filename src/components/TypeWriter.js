import React from 'react'

const TypeWriter = (props) => {
    return(
        <div>
            <span className='typewriter'> $(<span className='first'>{props.first}</span>).<span className='last'>{props.last}</span>(function({props.function})</span>
        </div>
    );
}

export default TypeWriter